# 📋 Инструкция по установке Git и загрузке кода на GitHub

## Шаг 1: Установить Git

1. **Откройте браузер** и перейдите:
   ```
   https://git-scm.com/download/win
   ```

2. **Скачайте Git** (автоматически выберется версия для Windows)

3. **Установите Git:**
   - Запустите скачанный файл
   - Нажимайте "Next" на всех шагах
   - Оставьте все настройки по умолчанию
   - Нажмите "Install"
   - Дождитесь окончания установки
   - Нажмите "Finish"

4. **Перезапустите PowerShell** (закройте и откройте заново)

## Шаг 2: Настроить Git (первый раз)

Откройте PowerShell в папке проекта и выполните:

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

Замените на ваши реальные данные (те, что используете на GitHub).

## Шаг 3: Инициализировать репозиторий

```powershell
cd C:\Users\Honor\Desktop\landing_1
git init
```

## Шаг 4: Добавить файлы

```powershell
git add .
```

## Шаг 5: Сделать первый commit

```powershell
git commit -m "Initial commit: лендинг Экономизм Private"
```

## Шаг 6: Подключить удаленный репозиторий

```powershell
git remote add origin https://github.com/akrich-commits/landing-e1.git
```

## Шаг 7: Запушить код

```powershell
git branch -M main
git push -u origin main
```

Вас попросят ввести логин и пароль GitHub (или токен доступа).

---

## Если возникли проблемы:

### Ошибка "remote origin already exists"
Выполните:
```powershell
git remote remove origin
git remote add origin https://github.com/akrich-commits/landing-e1.git
```

### Нужен токен доступа вместо пароля
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите срок действия и права (нужен `repo`)
4. Скопируйте токен и используйте его как пароль

---

**После установки Git напишите мне, и я помогу выполнить все команды!**
